export enum PlayerStates {
    IDLE = 'IDLE',
    RUNNING = 'RUNNING',
}

export enum SpiderStates {
    IDLE = 'IDLE',
    RUNNING = 'RUNNING',
}

export enum SawStates {
    RUNNING = 'RUNNING',
    BOUNCE_MOVE = 'BOUNCE_MOVE',
}
